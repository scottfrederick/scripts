if [ "$#" -lt 1 ]; then
  echo "Usage: $0 ENV_NAME
fi

TS_G_ENV=$1
UAA_ADMIN_PASSWORD=$(cat ~/Downloads/envs/${TS_G_ENV}.json | jq -r .pks_api.uaa_admin_password)
pks login -a api.pks.${TS_G_ENV}.cf-app.com -u admin -p ${UAA_ADMIN_PASSWORD} -k
